// Legacy frontend entrypoint - not used in the current demo app.
// Kept minimal so the TypeScript compiler passes without missing-module errors.

function App() {
  return null;
}

export default App;
